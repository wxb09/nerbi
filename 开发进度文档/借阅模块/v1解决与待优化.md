## 修复总结

### P0: 并发借阅问题

**修改文件**: [BorrowService.java](file:///d:/ai练/fast/backend/demo/src/main/java/com/neighbor/service/BorrowService.java#L83-L87)

创建借阅申请时检查物品是否可用。由于允许多个用户同时对同一物品发起申请，并发控制在审批阶段解决。

```java
if (item.getStatus() != ItemStatus.AVAILABLE) {
    log.warn("[BorrowService] 物品不可用: itemId={}, status={}", item.getId(), item.getStatus());
    throw new BusinessException(ErrorCode.ITEM_NOT_AVAILABLE);
}
```

### P1: 审批时二次检查物品状态

**修改文件**: [BorrowService.java](file:///d:/ai练/fast/backend/demo/src/main/java/com/neighbor/service/BorrowService.java#L130-L138)

审批通过时使用原子更新操作，确保物品状态从 `AVAILABLE` 变为 `BORROWED`。如果更新失败（返回0），说明物品已被其他借阅占用。

```java
int updatedItems = itemRepository.updateStatusIfMatch(itemId, ItemStatus.AVAILABLE, ItemStatus.BORROWED);
if (updatedItems == 0) {
    log.warn("[BorrowService] 审批时物品状态已变化，可能已被其他借阅占用: itemId={}, currentStatus={}", 
             itemId, borrow.getItem().getStatus());
    throw new BusinessException(ErrorCode.ITEM_NOT_AVAILABLE);
}
```

### P2: 取消借阅时恢复物品状态

**修改文件**: [BorrowService.java](file:///d:/ai练/fast/backend/demo/src/main/java/com/neighbor/service/BorrowService.java#L260-L264)

取消已审批的借阅时，将物品状态恢复为 `AVAILABLE`。

```java
if (borrow.getStatus() == BorrowStatus.APPROVED) {
    log.info("[BorrowService] 取消已审批的借阅，恢复物品状态: itemId={}", borrow.getItem().getId());
    borrow.getItem().setStatus(ItemStatus.AVAILABLE);
    itemRepository.save(borrow.getItem());
}
```

### 并发控制机制

- **乐观锁**: [Item.java](file:///d:/ai练/fast/backend/demo/src/main/java/com/neighbor/entity/Item.java#L19-L20) 已添加 `@Version` 注解
- **原子更新**: [ItemRepository.java](file:///d:/ai练/fast/backend/demo/src/main/java/com/neighbor/repository/ItemRepository.java#L55-L59) 的 `updateStatusIfMatch` 方法实现 CAS 风格的状态更新
  服务端异常，请稍后重试 (-1)

##

## 待优化位置

现在我理解了当前的实现情况：

1. 当前物品流转 ：显示用户作为借出者的所有借阅记录（包括待审批、已审批、进行中等状态）
2. 借阅记录 ：显示历史记录（已归还的）
   问题分析：
3. 多个借阅请求时，前端会显示多个待审批记录
4. 当同意一个后，其他待审批的不会自动处理
5. "当前物品流转"显示所有状态的记录，可能会很长
   让我进一步查看前端是如何获取和展示这些数据的。

