borrow.ts:14

POST http://localhost:8080/api/borrows 403 (Forbidden)

Promise.then

createBorrow @ borrow.ts:14

submitBorrow @ DetailView.vue:198

现在前端点击借阅 出现这个问题




ai:这是一个 403 Forbidden 错误，通常是认证/权限配置问题。让我检查后端的安全配置：

ai:我看到问题了。 /api/borrows 接口需要认证，但前端可能没有正确发送 token。让我检查前端的请求代码：
