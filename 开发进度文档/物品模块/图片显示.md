我需要修改 DetailView.vue 文件，主要解决两个问题：

1. 图片没显示出来：需要添加 getImageUrl 函数，确保图片路径正确
2. 物品主人显示"未知"：需要检查 DTO 结构，使用正确的字段名
首先，我需要检查 ItemDetailDTO 的结构，看看正确的字段名是什么。

backend\demo\src\main\java\com\neighbor\dto\ItemDetailDTO.java

Thought

现在我看到了问题：

1. 图片显示问题 ：
   
   - 前端期望的是``item.images`` 数组，每个元素有``url`` 属性
   - 但后端返回的``ItemDetailDTO`` 中``images`` 是``List<String>`` ，直接是URL字符串列表
2. 物品主人显示为未知 ：
   
   - 前端使用``item.ownerNickname`` 和``item.ownerAvatar``
   - 但后端返回的``ItemDetailDTO`` 中是``OwnerDTO owner`` 对象，包含``id`` ,``nickname`` ,``avatar`` 字段
我需要修改前端代码来正确处理这些数据结构。

DetailView.vue
frontend\src\views\DetailView.vue

+ 13 - 5

查看变更

Thought

## 详情页修复完成
### 修复的问题
1. 图片显示问题

- 原因 ：前端代码使用了错误的字段路径
- 修复 ：
  - 主图：从 item.mainImage 改为 item.images[0]
  - 缩略图：从 image.url 改为直接使用 image
  - 添加了 getImageUrl 函数处理图片路径
2. 物品主人显示问题

- 原因 ：前端代码使用了错误的字段路径
- 修复 ：
  - 昵称：从 item.ownerNickname 改为 item.owner.nickname
  - 头像：从 item.ownerAvatar 改为 item.owner.avatar
  - 主人ID：从 item.ownerId 改为 item.owner.id