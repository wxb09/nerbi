我看到问题了！数据库中 type 字段的默认值是小写 'normal'，但 PostType 枚举是大写的 NORMAL。当查询时，如果数据库中存储的是小写值，JPA 会尝试将其转换为枚举，但会失败。

让我检查 Post 实体中 type 字段的定义：

- ``@Enumerated(EnumType.STRING)`` 表示存储枚举的名称（大写）
- 但数据库默认值是小写 'normal'
这可能导致问题。但更重要的是，如果数据库中没有帖子数据，查询应该返回空列表而不是异常。

让我检查是否有其他问题。可能是 JPQL 查询中``p.type`` 的问题 - 在 JPQL 中应该使用枚举值而不是字符串。

让我修改查询，使用枚举比较或者直接用字符串。