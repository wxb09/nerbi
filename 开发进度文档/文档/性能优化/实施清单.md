# **性能优化实施清单**

&#x20;

## **Phase 1：前端 API 请求缓存层**

- &#x20;新建 **`frontend/src/utils/cache.ts`**（ApiCache 类）
- &#x20;修改 **`frontend/src/api.ts`**，集成请求拦截器缓存读取
- &#x20;修改 **`frontend/src/api.ts`**，集成响应拦截器缓存写入
- &#x20;验证：首页物品列表、论坛帖子列表命中缓存后不再重复请求

## **Phase 2：localStorage 基础数据缓存**

- &#x20;新建 **`frontend/src/utils/localCache.ts`**
- &#x20;分类数据接口接入 localCache（30 分钟 TTL）
- &#x20;社区数据接口接入 localCache（30 分钟 TTL）
- &#x20;用户基础信息接入 localCache
- &#x20;验证：刷新页面后分类/社区数据秒开，无网络请求

## **Phase 3：usePagination 缓存增强**

- &#x20;修改 **`frontend/src/composables/usePagination.ts`**，新增 **`cacheKey`** / **`cacheTTL`** 参数
- &#x20;首页物品列表启用 pagination 缓存
- &#x20;论坛帖子列表启用 pagination 缓存
- &#x20;写入/修改数据后调用 **`clearCache()`** 清除对应缓存
- &#x20;验证：翻页返回第一页走缓存，发布新内容后缓存自动清除

## **Phase 4：后端 Spring Cache 集成**

- **`pom.xml`** 添加 **`spring-boot-starter-cache`** + **`caffeine`** 依赖
- **`BackendApplication.java`** 添加 **`@EnableCaching`**
- &#x20;新建 **`config/CacheConfig.java`**（Caffeine 配置）
- **`PostService`** 添加 **`@Cacheable`** / **`@CacheEvict`**
- **`ItemService`** 添加 **`@Cacheable`** / **`@CacheEvict`**
- **`CategoryService`** 添加 **`@Cacheable`**
- **`CommunityService`** 添加 **`@Cacheable`**
- &#x20;验证：相同查询第二次命中缓存，数据库日志无重复 SQL

## **Phase 5：缓存失效策略完善**

- &#x20;物品发布/审核 → 清除物品列表缓存（前端 + 后端）
- &#x20;帖子发布/点赞/评论 → 清除帖子列表 + 详情缓存
- &#x20;管理员操作 → 清除对应业务全部缓存
- &#x20;后端添加定时任务：凌晨清理过期缓存
- &#x20;全链路联调验证：写操作后读取数据为最新值

***

**当前进度：0 / 24 项完成**
